const fs=require('fs');
function parseCsv(text){const rows=[];let row=[],cell='',q=false;for(let i=0;i<text.length;i++){const ch=text[i],nx=text[i+1];if(q){if(ch==='"'&&nx==='"'){cell+='"';i++;}else if(ch==='"'){q=false;}else cell+=ch;}else{if(ch==='"')q=true;else if(ch===','){row.push(cell);cell='';}else if(ch==='\n'){row.push(cell);rows.push(row);row=[];cell='';}else if(ch!=='\r')cell+=ch;}} if(cell.length||row.length){row.push(cell);rows.push(row);} return rows;}
function count(file, header){const rows=parseCsv(fs.readFileSync('_import_koc_laforin/'+file,'utf8')); const body=header?rows.slice(1):rows.slice(1); const products={}; let nonempty=0; for(const r of body){const name=header?r[1]:r[0]; if(!name) continue; nonempty++; const p=header?r[13]:r[7]; products[p||'(blank)']=(products[p||'(blank)']||0)+1;} console.log(file,'rows',rows.length,'validName',nonempty,'products',products);}
count('danh_sach_koc.csv',false); count('koc_hop_tac.csv',true); count('koc_free_cast.csv',true);
